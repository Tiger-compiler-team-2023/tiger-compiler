
function a()=
let
    var a := 2 + 3 * 2
    var b := 2 + (3 * 2)
    var c := (2 + 3) * 2

    if (a = b) then (print("a = b ok\n"))
    else  (print("a != b erreur\n"))

    if (a <> c) then (print("a != c ok\n"))
    else  (print("a = c erreur\n"))

    if (4 > 6 * (-1)) then print("'*' est prioritaire sur '>'")

    var d := 120 / 5 / 4 / 3 / 2
    if (d = 1) then (print("la division est associative gauche"))

in end