let
    var N := 5
    type tblty = array of int
    var tbl := tblty [N] of 0
    var i := 0
in
    printi(N);
    while i < N do (
        tbl[i] := i;
        i := i + 1
    );
    printi(tbl[2])
end